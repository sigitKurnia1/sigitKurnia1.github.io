# assets/

Drop your real files here. The site references these names:

| File           | Purpose                                  | Status                          |
|----------------|------------------------------------------|---------------------------------|
| `resume.pdf`   | Linked by the "Download Resume" button   | **PLACEHOLDER — replace this**   |
| `favicon.svg`  | Browser tab icon                         | Provided (edit if you want)     |
| `favicon.ico`  | Fallback icon for older browsers         | Optional — add if you have one  |
| `og-image.png` | 1200×630 social share preview image      | **Add for nice link previews**  |
| `profile.jpg`  | Your photo (not yet placed in the page)  | Optional                        |

## What you need to do
1. Export your CV as **resume.pdf** and place it in this folder (overwrites the placeholder).
2. (Optional) Create a **1200×630 og-image.png** so shared links show a preview card.
3. (Optional) Add a real **favicon.ico**.

The current `resume.pdf` is a tiny placeholder so the link doesn't 404 — replace it.
